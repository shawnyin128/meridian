# Lab Research Prior Review

Feature: Lab research prior grounding

## Context/Test Plan

Goal: make Lab preserve research-prior grounding for method, prompt, metric,
evaluation, baseline, ablation, probe, and failure decisions without turning Lab
into a coding workflow or heavy retrieval engine.

Planning source:

- `docs/superpowers/specs/2026-06-01-lab-research-prior-design.md`

Required evidence:

- Lab skill classifies Research Prior state with `needed`, `checked`,
  `missing`, `deferred`, and `not_needed`.
- Lab skill treats `missing` prior as an under-grounded user-confirmation gate,
  not as checked evidence.
- Thread and experiment templates contain compact Research Prior blocks.
- State/use-case docs explain Research Prior and the missing-prior gate.
- Eval cases and rubric cover LLM-as-Judge prompt design, metric/failure
  interpretation, ablation/probe design, missing prior, and local engineering
  `not_needed`.
- No Lab MCP, CLI, daemon, database, coding ownership, or git convergence
  behavior is added.

## Developer Round

Implemented artifacts:

- `plugins/codex/meridian/skills/lab/SKILL.md`
- `plugins/claude-code/meridian/skills/lab/SKILL.md`
- `docs/research-dev-state-model.md`
- `docs/research-dev-use-cases.md`
- `docs/research-dev-mvp-plan.md`
- `src/meridian/templates/research-dev/thread.md`
- `src/meridian/templates/research-dev/experiment.md`
- `eval/cases/research_dev_state_model.jsonl`
- `eval/cases/research_dev_research_prior.jsonl`
- `eval/rubrics/research_dev_state_model_quality.md`
- `eval/rubrics/research_dev_research_prior_quality.md`
- `tests/test_cli.py`

Design decisions:

- Research Prior is embedded in thread/node and experiment state rather than
  added as a fifth top-level Lab object.
- There is no numeric cap on prior lookup. The stopping condition is that each
  research-bearing decision has a prior status.
- `missing` remains `missing` after user confirmation. Confirmation accepts the
  under-grounded risk; it does not create evidence.
- Pure local engineering is `not_needed` unless it changes research
  interpretation.

## Evaluator Round

Validated behavior:

- TDD red test failed before implementation:
  `PYTHONPATH=src python3 -m unittest tests.test_cli.CliTests.test_lab_research_prior_assets_parse`
  failed because `Research Prior` was absent from the Lab skill.
- Targeted research-prior test passed after implementation.
- Related Lab/skill boundary tests passed:
  - `test_meridian_product_skill_behavior_boundaries`
  - `test_meridian_plugin_skill_copies_match_repo_skills`
  - `test_meridian_plugin_skill_frontmatter_is_loader_safe`
  - `test_research_dev_state_model_assets_parse`
  - `test_research_dev_zero_candidate_idea_replay_contract`
  - `test_research_dev_eval_assets_parse`
  - `test_research_dev_longitudinal_replay_assets_parse`
- Full unit suite passed: `155 tests`.
- Compile check passed for `src` and `tests`.
- `git diff --check` passed.
- Meridian framework check passed: 7 categories pass, 0 warn, 0 fail.
- Active external Paper Wiki gates passed:
  - `wiki lint`: pass, 6 findings.
  - `source-audit`: 247 sources, 0 missing, 0 SHA mismatches, 0 duplicate SHA
    groups.
  - `catalog`: 243 catalog entries.
- AGENTS project-map drift hook passed after removing transient ignored
  repo-local `wiki/` artifacts generated by an incorrect validation command.

Residual process-state note:

- Arbor process-state still reports historical F1/F37 review-evidence findings.
  No finding points to this Lab research-prior feature.

## Convergence Round

Converged.

The implementation matches the design: Lab now records research-prior state for
research-bearing methods, prompts, metrics, evals, ablations, probes, baselines,
and failure interpretations; `missing` prior is preserved as an under-grounded
user-confirmation gate; and Lab still hands code, tests, commits, release, and
convergence to normal development workflows.

## Release Round

Release evidence:

- `PYTHONPATH=src python3 -m unittest discover -s tests`: pass.
- `PYTHONPYCACHEPREFIX=/private/tmp/meridian-lab-prior-pycache PYTHONPATH=src python3 -m compileall src tests`: pass.
- `git diff --check`: pass.
- `PYTHONPATH=src python3 -m meridian framework-check --project-root . --library-root /Users/shawn/Desktop/paper-wiki`: pass.
- Active Paper Wiki lint/source-audit/catalog: pass.
- AGENTS drift hook: pass.

Implementation commit:

- `224dc20 feat: add lab research prior grounding`

Version finalization:

- Bumped release surfaces from `0.4.5` to `0.4.6`:
  - `VERSION`
  - `pyproject.toml`
  - `src/meridian/__init__.py`
  - `plugins/codex/meridian/.codex-plugin/plugin.json`
  - `plugins/claude-code/meridian/.claude-plugin/plugin.json`
  - release version alignment test
- Version smoke: `PYTHONPATH=src python3 -m meridian --version` returned
  `meridian 0.4.6`.
- Targeted version and Lab release tests passed.
- Full unit suite passed after version bump: `155 tests`.
- `PYTHONPYCACHEPREFIX=/private/tmp/meridian-0.4.6-pycache PYTHONPATH=src python3 -m compileall src tests`: pass.
- `git diff --check`: pass.
- `PYTHONPATH=src python3 -m meridian framework-check --project-root . --library-root /Users/shawn/Desktop/paper-wiki`: pass.
- Active external Paper Wiki lint/source-audit/catalog: pass.
- AGENTS drift hook: pass.
- Arbor process-state still reports historical F1/F37 review-evidence findings;
  no finding points to this release.

Version commit pending at the time this release note was updated.

## Feedback Repair Round: Research Prior Gate

Feedback:

- The 0.4.6 Lab Research Prior behavior was still too soft in practice. Agents
  often treated Paper Wiki/MCP retrieval as optional background instead of an
  action required before shaping research-bearing plans.

Diagnosis:

- The rubric and eval assets already treated skipped Wiki grounding as a hard
  failure.
- The Lab skill still used soft language such as "when ... matter", "likely",
  and "Preferred MCP tools".
- That wording left too much room for agents to classify LLM-as-Judge prompt,
  metric, baseline, probe, method, ablation, or failure decisions from chat
  intuition alone.

Repair scope:

- Add a front-loaded `Research Prior Gate` to the Lab skill.
- Require a scan for `method`, `prompt`, `metric`, `eval`, `ablation`, `probe`,
  `failure`, and `baseline` slots before finalizing a Lab plan.
- Make `meridian.context` the default grounding path for every
  research-bearing slot, with targeted `meridian.read` and `meridian.trace`
  when returned pages or provenance could change the decision.
- Preserve `missing` as a valid under-grounded state instead of treating it as
  a failed workflow.
- Keep local engineering chores `not_needed` so Lab does not retrieve Wiki for
  every implementation detail.

## Release Round: 0.4.7

Release evidence:

- `PYTHONPATH=src python3 -m unittest discover -s tests`: pass, 155 tests.
- `PYTHONPYCACHEPREFIX=/private/tmp/meridian-0.4.7-pycache PYTHONPATH=src python3 -m compileall src tests`: pass.
- `git diff --check`: pass.
- `PYTHONPATH=src python3 -m meridian framework-check --project-root . --library-root /Users/shawn/Desktop/paper-wiki`: pass, 7 categories pass, 0 warn, 0 fail.
- `PYTHONPATH=src python3 -m meridian wiki lint --wiki-root /Users/shawn/Desktop/paper-wiki/wiki`: pass, 6 findings.
- `PYTHONPATH=src python3 -m meridian wiki source-audit --wiki-root /Users/shawn/Desktop/paper-wiki/wiki`: pass, 247 sources, 0 missing, 0 SHA mismatches, 0 duplicate SHA groups.
- `PYTHONPATH=src python3 -m meridian wiki catalog --wiki-root /Users/shawn/Desktop/paper-wiki/wiki`: pass, 243 catalog entries.

Version surfaces:

- `VERSION`: `0.4.7`
- `pyproject.toml`: `0.4.7`
- Python package version: `0.4.7`
- Codex plugin manifest: `0.4.7`
- Claude Code plugin manifest: `0.4.7`
