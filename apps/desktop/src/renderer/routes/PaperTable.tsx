import './shell.css'

/** The paper library routing is only responsible for assembling the page-level shell; the complete table function is held by the shared module. */
export * from '../components/paper-table/PaperTable.js'
